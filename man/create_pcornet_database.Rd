% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcornet.R
\name{create_pcornet_database}
\alias{create_pcornet_database}
\title{Create PCORnet CDM Synthetic Database}
\usage{
create_pcornet_database(
  n_patients = 100,
  mode = c("enhanced", "random", "synthea"),
  current_date = Sys.Date(),
  seed = 42,
  save_to_disk = TRUE,
  output_dir = ".",
  synthea_dir = NULL,
  profile_weights = NULL,
  sources = NULL
)
}
\arguments{
\item{n_patients}{Number of synthetic patients to generate (default: 100)}

\item{mode}{Generation mode: "enhanced" (clinical profiles), "random", or "synthea"}

\item{current_date}{Reference date for data generation (default: Sys.Date())}

\item{seed}{Random seed for reproducibility (default: 42)}

\item{save_to_disk}{Save databases to disk files (default: TRUE)}

\item{output_dir}{Directory for output files (default: ".")}

\item{synthea_dir}{Path to Synthea CSV output (required when mode = "synthea")}

\item{profile_weights}{Named list to override default profile distribution}

\item{sources}{Named list of source systems for MPI. Each source should have
id_field, description, and null_rate. Use DEFAULT_SOURCES as a template.}
}
\value{
List with:
\item{cdw}{DBI connection to Clinical Data Warehouse}
\item{mpi}{DBI connection to Master Patient Index}
\item{summary}{Generation statistics}
}
\description{
Generates synthetic patient data following PCORnet CDM schema with optional
clinical coherence using patient profiles.
}
\examples{
\dontrun{
# Basic usage
dbs <- create_pcornet_database()

# Custom patient count
dbs <- create_pcornet_database(n_patients = 5000)

# Random mode (no clinical coherence)
dbs <- create_pcornet_database(mode = "random")

# Custom profile distribution
dbs <- create_pcornet_database(
  profile_weights = list(diabetic = 0.3, healthy = 0.7)
)

# Custom source systems
dbs <- create_pcornet_database(
  sources = list(
    EPIC = list(id_field = "EPIC_PAT_ID", description = "Epic EHR", null_rate = 0.1),
    CERNER = list(id_field = "CERNER_ID", description = "Cerner EHR", null_rate = 0.3)
  )
)
}

}
