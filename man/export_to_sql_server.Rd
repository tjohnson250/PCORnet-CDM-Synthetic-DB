% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_sql_server.R
\name{export_to_sql_server}
\alias{export_to_sql_server}
\title{Export PCORnet databases to SQL Server}
\usage{
export_to_sql_server(
  dbs,
  server,
  database,
  uid = NULL,
  pwd = NULL,
  driver = "ODBC Driver 17 for SQL Server",
  port = 1433,
  schema = "dbo",
  overwrite = FALSE,
  tables = NULL,
  include_cdw = TRUE,
  include_mpi = TRUE,
  trusted_connection = FALSE,
  connection_string = NULL,
  batch_size = 10000,
  verbose = TRUE
)
}
\arguments{
\item{dbs}{List containing \verb{$cdw} and \verb{$mpi} database connections (from
\code{create_pcornet_database()} or \code{load_pcornet_database()})}

\item{server}{SQL Server hostname (e.g., "localhost" or "server.database.windows.net")}

\item{database}{Target database name}

\item{uid}{Username for SQL Server authentication}

\item{pwd}{Password for SQL Server authentication}

\item{driver}{ODBC driver name (default: "ODBC Driver 17 for SQL Server")}

\item{port}{SQL Server port (default: 1433)}

\item{schema}{Target schema for tables (default: "dbo")}

\item{overwrite}{If TRUE, overwrite existing tables. If FALSE, fail if tables exist.
(default: FALSE)}

\item{tables}{Character vector of specific tables to export, or NULL for all tables
(default: NULL)}

\item{include_cdw}{Export CDW tables (default: TRUE)}

\item{include_mpi}{Export MPI tables (default: TRUE)}

\item{trusted_connection}{Use Windows integrated authentication instead of uid/pwd
(default: FALSE)}

\item{connection_string}{Optional full ODBC connection string. If provided, overrides
individual connection parameters.}

\item{batch_size}{Number of rows to write at a time for large tables (default: 10000)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
Invisible list with export statistics (tables exported, row counts, any errors)
}
\description{
Transfers CDW and MPI tables from DuckDB to a Microsoft SQL Server database.
}
\details{
Requires the \code{odbc} package to be installed. Install the Microsoft ODBC Driver:
\itemize{
\item Windows: Download from Microsoft
\item Mac: \verb{brew install microsoft/mssql-release/msodbcsql17}
\item Linux: See Microsoft documentation for your distribution
}
}
\examples{
\dontrun{
# Generate synthetic data
dbs <- create_pcornet_database(n_patients = 100)

# Export to SQL Server with username/password
export_to_sql_server(
  dbs,
  server = "localhost",
  database = "PCORnet_Dev",
  uid = "sa",
  pwd = "YourPassword123"
)

# Export with Windows authentication
export_to_sql_server(
  dbs,
  server = "localhost",
  database = "PCORnet_Dev",
  trusted_connection = TRUE
)

# Export only specific tables
export_to_sql_server(
  dbs,
  server = "localhost",
  database = "PCORnet_Dev",
  uid = "sa",
  pwd = "YourPassword123",
  tables = c("DEMOGRAPHIC", "ENCOUNTER", "DIAGNOSIS")
)

# Export to Azure SQL Database
export_to_sql_server(
  dbs,
  server = "myserver.database.windows.net",
  database = "PCORnet_Dev",
  uid = "admin@myserver",
  pwd = "YourPassword123"
)
}

}
